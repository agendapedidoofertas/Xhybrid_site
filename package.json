{
  "name": "xhybrid-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "bash scripts/dev.sh --port 8000",
    "build": "echo \"static site + PHP admin, nothing to build\"",
    "preview": "bash scripts/dev.sh --port 8000"
  }
}
